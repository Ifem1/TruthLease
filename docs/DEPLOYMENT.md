# Deployment evidence

No canonical Studionet deployment is recorded in this repository yet. This document intentionally contains no invented address, transaction, finality, or runtime result.

Before submission, deploy the exact `contracts/truth_lease.py` source with official GenLayer tooling, record the public contract address and transaction, then capture a confirmed path, a fail-closed path, and a lifecycle path. Record the deployment commit and compare its contract blob SHA with `main`; redeploy if that contract file changes.
